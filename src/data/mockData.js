// Deprecated: the app now reads and writes through Supabase.
// These exports remain only as default seed values for the new data layer.
export {
  defaultAboutSections as aboutSections,
  defaultHeroSlides as heroSlides,
  defaultNavItems as navItems,
  defaultNewsItems as newsItems,
  defaultQuickStats as quickStats,
  defaultServices as services,
  defaultSiteMeta as siteMeta,
  defaultTrackStatusItems as trackStatusItems,
} from "./defaultContent";
