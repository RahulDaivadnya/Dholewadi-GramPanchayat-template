# UI Template

Reusable Gram Panchayat website UI template extracted from the existing project.

## Goals

- Keep the original app untouched
- Reuse only UI structure and styling patterns
- Replace backend dependencies with mock data
- Keep components composable and page-agnostic

## Run

```bash
npm install
npm run dev
```
